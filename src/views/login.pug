extends base.pug
block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(name="username", placeholder="Username", required)
        input(name="password", placeholder="Password", required)
        input(value="Login", type="submit")
    hr
    div
        span Don't you have any Account?
        a(href="/join") Create Account &rarr;